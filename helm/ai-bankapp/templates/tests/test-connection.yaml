apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-bankapp.fullname" . }}-test-connection"
  labels:
    {{- include "ai-bankapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-bankapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
